---
interface Props {
    className?: string
}
const { className } = Astro.props
---
<div class={`id_column grid grid-cols-1 justify-between gap-4 lg:grid-cols-2 2xl:grid-cols-4 ${className}`}>
    <slot />
</div>
