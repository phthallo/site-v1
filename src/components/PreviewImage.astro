---
interface Props {
    image: string;
    altText: string;
}

const { image, altText } = Astro.props;
---

<img class="m-auto my-4 block h-72 w-full rounded-2xl object-cover opacity-80 transition-all duration-300 hover:opacity-100" src={image} alt={altText} />
