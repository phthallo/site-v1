---
interface Props {
    image: string;
    altText: string;
}

const { image, altText } = Astro.props
---

<img class = "block m-auto w-full my-4 rounded-2xl opacity-80 transition-all duration-300 hover:opacity-100" src = {image} alt = {altText}></img>
